name: block
description: Blob-compatible binary data blocks with platform-optimized implementations for web and io.
version: 1.2.0
repository: https://github.com/medz/block

topics:
  - binary
  - blob
  - streams
  - web
  - io

environment:
  sdk: ^3.10.0

dependencies:
  web: ^1.1.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.29.0
  benchmark_harness: ^2.4.0
  coal: ^0.0.6
